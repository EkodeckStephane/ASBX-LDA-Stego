# Runtime dependencies for the SPE reproducibility scripts.
# Tested locally on Python 3.13 with numpy 2.2.6 and matplotlib 3.10.5.
# The ASBX codec is imported from ../../experiments/src/asbc.

numpy>=2.2,<2.3
matplotlib>=3.10,<3.11
brotli>=1.1,<1.2
zstandard>=0.23,<0.24

# Optional LDA-training backends.  The manuscript capacity and practical
# benchmarks do not train LDA, but lda_trainer.py can use these packages.
scikit-learn>=1.7,<1.8
gensim>=4.3,<4.4
nltk>=3.9,<3.10
